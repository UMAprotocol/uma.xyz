import { Features } from "@/components/OsnapV2/Features";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  component: Features,
};

export default meta;

type Story = StoryObj<typeof Features>;

const Template: Story = {};

export const Default: Story = {
  ...Template,
};
