import { Scaling } from "@/components/Osnap/Scaling";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  component: Scaling,
};

export default meta;

type Story = StoryObj<typeof Scaling>;

const Template: Story = {};

export const Default: Story = {
  ...Template,
};
