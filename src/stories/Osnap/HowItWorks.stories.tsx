import { HowItWorks } from "@/components/Osnap/HowItWorks";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta = {
  component: HowItWorks,
};

export default meta;

type Story = StoryObj<typeof HowItWorks>;

const Template: Story = {};

export const Default: Story = {
  ...Template,
};
